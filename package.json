{
  "name": "rig",
  "version": "0.1.2",
  "description": "Builds torad-labs/llama.cpp per GPU and brings model heads up, gated and measured — the local runtime behind splice's OpenAI-compatible provider rows",
  "private": true,
  "type": "module",
  "bin": {
    "rig": "src/main.ts"
  },
  "scripts": {
    "rig": "bun run src/main.ts",
    "test": "bun test",
    "lint": "bunx biome check --error-on-warnings . && bun run scripts/lint-orthogonality.ts && bunx tsc --noEmit",
    "format": "bunx biome check --write .",
    "build": "bun build --compile --target=bun-linux-x64 --outfile dist/rig src/main.ts"
  },
  "dependencies": {
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
