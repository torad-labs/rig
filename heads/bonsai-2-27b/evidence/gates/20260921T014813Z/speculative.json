{
  "plain": {
    "answers": [
      "TCP congestion control is a mechanism used to prevent network saturation by dynamically adjusting the amount of data sent based on network conditions. It operates primarily through four phases: slow start, congestion avoidance, fast retransmit, and fast recovery.\n\nInitially, during **slow start**, the sender begins with a small window size and doubles it after each round-trip time (RTT) until a threshold is reached. This phase quickly increases throughput but risks overwhelming the network. Once the threshold is hit, the algorithm transitions to **congestion avoidance**, where the window size increases linearly by one segment per RTT. This gradual increase helps maintain stability without causing excessive load.\n\nIf a packet is lost, indicated by a duplicate ACK or timeout, the sender assumes congestion. In **fast retransmit**, the sender immediately retransmits the missing packet without waiting for a timeout. Simultaneously, the congestion window is halved to reduce load. **Fast recovery** then allows the sender to resume sending at a reduced rate, typically increasing the window by one segment per RTT until the original window size is restored.\n\nThis feedback loop ensures that TCP adapts to varying network conditions, balancing throughput and reliability while minimizing packet loss and latency.",
      "```python\ndef parse_iso8601_date(date_string: str) -> tuple[int, int, int]:\n    \"\"\"\n    Parse an ISO-8601 date string into a tuple of (year, month, day).\n\n    The input string is expected to be in the format 'YYYY-MM-DD'.\n    This function does not use any external libraries.\n\n    Args:\n        date_string: A string representing an ISO-8601 date (e.g., '2023-10-05').\n\n    Returns:\n        A tuple containing the year, month, and day as integers.\n\n    Raises:\n        ValueError: If the string is not in the correct format or contains invalid characters.\n    \"\"\"\n    if not isinstance(date_string, str):\n        raise ValueError(\"Input must be a string.\")\n\n    parts = date_string.split('-')\n    \n    if len(parts) != 3:\n        raise ValueError(\"Date string must be in the format 'YYYY-MM-DD'.\")\n\n    year_str, month_str, day_str = parts\n\n    if not year_str.isdigit() or not month_str.isdigit() or not day_str.isdigit():\n        raise ValueError(\"Year,",
      "Here are the planets of the solar system in order from the Sun, along with a key fact about each:\n\n1. **Mercury**  \n   *Fact:* Mercury has no atmosphere, so its surface temperature swings wildly from about 430°C (806°F) during the day to -180°C (-292°F) at night.\n\n2. **Venus**  \n   *Fact:* Venus is the hottest planet in the solar system due to its thick, toxic atmosphere of carbon dioxide and sulfuric acid clouds, which creates a runaway greenhouse effect.\n\n3. **Earth**  \n   *Fact:* Earth is the only known planet in the solar system that supports life, largely due to its abundant liquid water and protective magnetic field.\n\n4. **Mars**  \n   *Fact:* Mars has the largest volcano in the solar system, Olympus Mons, which is about three times taller than Mount Everest and roughly the size of the state of Arizona.\n\n5. **Jupiter**  \n   *Fact:* Jupiter is the largest planet in the solar system, so massive that all the other planets combined could fit inside it. It also has a Great Red Spot, a storm larger than Earth that has been raging for centuries."
    ],
    "tps": 75.89699190537179,
    "draftN": 0,
    "accepted": 0
  },
  "drafted": {
    "answers": [
      "TCP congestion control is a mechanism used to prevent network saturation by dynamically adjusting the amount of data sent based on network conditions. It operates primarily through four phases: slow start, congestion avoidance, fast retransmit, and fast recovery.\n\nInitially, during **slow start**, the sender begins with a small window size and doubles it after each round-trip time (RTT) until a threshold is reached. This phase quickly increases throughput but risks overwhelming the network. Once the threshold is hit, the algorithm transitions to **congestion avoidance**, where the window size increases linearly by one segment per RTT. This gradual increase helps maintain stability without causing excessive load.\n\nIf a packet is lost, indicated by a duplicate ACK or timeout, the sender assumes congestion. In **fast retransmit**, the sender immediately retransmits the missing packet without waiting for a timeout. Simultaneously, the congestion window is halved to reduce load. **Fast recovery** then allows the sender to resume sending at a reduced rate, gradually increasing the window size again.\n\nThis feedback loop ensures that TCP adapts to varying network conditions, balancing throughput and reliability while minimizing packet loss and latency.",
      "```python\ndef parse_iso8601_date(date_str: str) -> tuple[int, int, int]:\n    \"\"\"\n    Parse an ISO-8601 date string into a tuple of (year, month, day).\n\n    The input string is expected to be in the format 'YYYY-MM-DD'.\n    This function does not use any external libraries.\n\n    Args:\n        date_str: A string representing an ISO-8601 date (e.g., '2023-10-05').\n\n    Returns:\n        A tuple containing the year, month, and day as integers.\n\n    Raises:\n        ValueError: If the string is not in the correct format or contains invalid characters.\n    \"\"\"\n    if not isinstance(date_str, str):\n        raise ValueError(\"Input must be a string.\")\n\n    parts = date_str.split('-')\n    if len(parts) != 3:\n        raise ValueError(\"Date string must be in the format 'YYYY-MM-DD'.\")\n\n    year_str, month_str, day_str = parts\n\n    if not (year_str.isdigit() and month_str.isdigit() and day_str.isdigit()):\n        raise ValueError(\"Year, month,",
      "Here are the planets of the solar system in order from the Sun, along with a key fact about each:\n\n1. **Mercury**  \n   *Fact:* Mercury has no atmosphere, so its surface temperature swings wildly from about 430°C (806°F) during the day to -180°C (-292°F) at night.\n\n2. **Venus**  \n   *Fact:* Venus is the hottest planet in the solar system due to its thick, toxic atmosphere of carbon dioxide and sulfuric acid clouds, which creates a runaway greenhouse effect.\n\n3. **Earth**  \n   *Fact:* Earth is the only known planet in the solar system that supports life, largely due to its abundant liquid water and protective magnetic field.\n\n4. **Mars**  \n   *Fact:* Mars has the largest volcano in the solar system, Olympus Mons, which is about three times taller than Mount Everest and roughly the size of the state of Arizona.\n\n5. **Jupiter**  \n   *Fact:* Jupiter is the largest planet in the solar system and has a massive storm called the Great Red Spot that has been raging for at least 300 years.\n\n6. **Saturn**  \n   *Fact"
    ],
    "tps": 103.37427694344315,
    "draftN": 608,
    "accepted": 443
  },
  "verdict": {
    "identical": [
      false,
      false,
      false
    ],
    "ran": true,
    "fast": true
  }
}